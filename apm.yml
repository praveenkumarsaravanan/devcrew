name: devcrew
version: "1.0.1"
description: "AI engineering team simulation -- skills, MCP servers, rules, hooks, and agents for Cursor and GitHub Copilot"
target:
  copilot: {}
  cursor:
    instructions: .cursor/rules/
    prompts: .cursor/commands/
scripts:
  setup: "bash scripts/setup.sh"
  release: "bash .apm/skills/git-release-tag/scripts/release.sh"
