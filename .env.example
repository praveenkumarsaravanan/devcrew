# Copy this file to .env and fill in your values.
# The setup script (scripts/setup.sh) sources .env automatically.
# .env is gitignored — your values never leave your machine.

GIT_HOST="github.example.com"
GIT_API_URL="https://${GIT_HOST}/api/v3"
GITHUB_TOKEN=""
